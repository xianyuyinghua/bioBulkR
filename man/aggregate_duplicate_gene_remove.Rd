% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_duplicate_gene_remove.R
\name{aggregate_duplicate_gene_remove}
\alias{aggregate_duplicate_gene_remove}
\title{Resolve duplicated genes in an expression table}
\usage{
aggregate_duplicate_gene_remove(
  data,
  gene_col,
  sample_col,
  duplicate_remove_method = "mean-max"
)
}
\arguments{
\item{data}{A \code{data.frame} containing a gene identifier column and one or
more expression sample columns.}

\item{gene_col}{A single character string giving the name of the gene
identifier column in \code{data}.}

\item{sample_col}{A character vector giving the names of expression columns
in \code{data}.}

\item{duplicate_remove_method}{A character string specifying how duplicated
genes are resolved. The default, \code{"mean-max"}, retains the row having the
largest mean across \code{sample_col}. Other values are passed to
\code{\link[base:match.fun]{base::match.fun()}}, for example, \code{"mean"}, \code{"median"}, or \code{"max"}, and
are used to aggregate each sample column by gene.}
}
\value{
A \code{data.frame} with one row per gene. The first column has the name
supplied in \code{gene_col}, followed by the columns supplied in \code{sample_col}.
}
\description{
Produce one expression profile per gene by either retaining the row with
the highest mean expression or aggregating duplicated rows with a summary
function.
}
\details{
Only \code{gene_col} and \code{sample_col} are retained in the output. Sample
columns are converted to a numeric matrix before duplicate processing, so
their values must be coercible to numeric.

With \code{duplicate_remove_method = "mean-max"}, a mean is calculated across
the selected sample columns for every input row using \code{na.rm = TRUE}.
Within each gene, the row with the largest mean is retained; its original
expression values are returned unchanged after numeric conversion.

For any other method, \code{\link[stats:aggregate]{stats::aggregate()}} applies the selected function
independently to every sample column for each gene, also using
\code{na.rm = TRUE}.
}
\examples{
expression_data <- data.frame(
  gene = c("TP53", "TP53", "BRCA1"),
  sample_1 = c(4, 8, 5),
  sample_2 = c(6, 10, 7)
)

aggregate_duplicate_gene_remove(
  expression_data,
  gene_col = "gene",
  sample_col = c("sample_1", "sample_2")
)

aggregate_duplicate_gene_remove(
  expression_data,
  gene_col = "gene",
  sample_col = c("sample_1", "sample_2"),
  duplicate_remove_method = "mean"
)
}
