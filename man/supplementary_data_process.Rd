% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supplementary_data_process.R
\name{supplementary_data_process}
\alias{supplementary_data_process}
\title{Interactively process GEO supplementary expression data}
\usage{
supplementary_data_process(
  file_path,
  read_data_function = read_data,
  sample_modle = "^GSM"
)
}
\arguments{
\item{file_path}{A single character string giving the path to a
supplementary-data file. Files ending in \code{.tar} are handled as multi-file
archives; other supported formats are passed to \code{\link[=read_data]{read_data()}}.}

\item{read_data_function}{A function used by \code{check_column_consistency()} to
read individual files extracted from a \code{.tar} archive. It must accept a
file path and return a tabular object. The default is \code{\link[=read_data]{read_data()}}.}

\item{sample_modle}{A regular-expression pattern used to select sample files
recursively from an extracted \code{.tar} archive. The default, \code{"^GSM"},
selects filenames beginning with a GEO Sample accession. The argument name
is retained as \code{sample_modle} for compatibility.}
}
\value{
A named list containing:
\describe{
\item{\code{gene_exp_type}}{The expression-data type entered by the user, such
as \code{"Count"}, \code{"TPM"}, or \code{"FPKM"}.}
\item{\code{gene_expression_data}}{A table containing gene identifiers and
expression values. For a multi-file archive, sample tables are merged by
their common gene identifiers and the gene column is named \code{Gene_ID}.}
\item{\code{gene_another_data}}{Optional additional columns selected by the
user, or \code{NULL} when no additional information is requested.}
}
}
\description{
Process either a supplementary-data archive containing one expression file
per sample or a single supplementary table. The function interactively asks
the user to identify gene, expression, and optional annotation columns and
returns standardized expression data.
}
\details{
For a \code{.tar} input, the archive is extracted into a directory next
to \code{file_path} whose name is formed by removing the \code{.tar} suffix. Nested
\code{.gz} sample files are decompressed in place with \code{\link[R.utils:compressFile]{R.utils::gunzip()}}. The
selected sample files are then passed to \code{check_column_consistency()}. If
their columns differ, the function displays the consistent and
inconsistent groups and stops so that the files can be reviewed.

When archive columns are consistent, the function proposes character
columns as gene-ID candidates and integer columns as expression-value
candidates. The user confirms the desired columns and expression type.
Only genes shared by every sample file are included in the merged
expression table.

For a non-\code{.tar} input, \code{\link[=read_data]{read_data()}} imports the table. The user supplies
comma-separated patterns for the gene-ID and expression columns. Patterns
are matched against column names with \code{\link[base:grep]{base::grep()}}, so a pattern can
select more than one column. Optional additional-column patterns use the
same comma-separated format.

The function is intended for an interactive R session because it uses
\code{\link[base:readline]{base::readline()}}. Archive extraction creates files and directories next
to the input path, and gzip decompression can remove the compressed sample
files after successful extraction.
}
\examples{
\dontrun{
processed <- supplementary_data_process(
  file_path = "GSE00000_RAW.tar",
  sample_modle = "^GSM"
)

expression_data <- processed$gene_expression_data
}
}
\seealso{
\code{\link[=read_data]{read_data()}}, \code{\link[R.utils:compressFile]{R.utils::gunzip()}}, \code{\link[utils:untar]{utils::untar()}}
}
