% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_gset.R
\name{download_gset}
\alias{download_gset}
\title{Download and load a GEO data set}
\usage{
download_gset(data_id, destdir)
}
\arguments{
\item{data_id}{A single GEO accession supplied as a character string,
beginning with \code{"GSE"} or \code{"GPL"} (for example, \code{"GSE1009"}).}

\item{destdir}{A character string giving the directory in which downloaded
files are stored and from which manually downloaded files are read.}
}
\value{
On a successful automatic download, the object returned by
\code{\link[GEOquery:getGEO]{GEOquery::getGEO()}} (typically a list of \code{ExpressionSet} objects for a
\code{GSE} accession or a \code{GPL} object for a platform accession). When a
manually downloaded GPL annotation file is loaded, a \code{data.frame} is
returned. If the data cannot be downloaded or loaded, \code{NULL} is returned.
}
\description{
Download a Gene Expression Omnibus (GEO) Series (\code{GSE}) or Platform
(\code{GPL}) record with \code{\link[GEOquery:getGEO]{GEOquery::getGEO()}}. If the automatic download fails,
the function prompts the user to download the required file manually and
then attempts to load it from \code{destdir}.
}
\details{
For a \code{GSE} accession, the manually downloaded Series Matrix file should be
placed in \code{destdir}. For a \code{GPL} accession, place the uncompressed platform
annotation text file (a file whose name starts with \code{GPL} and ends in
\code{.txt}) in \code{destdir}. Because the fallback uses an interactive prompt, this
function is intended primarily for interactive use.

The GEO download timeout is set to 120 seconds for the duration of
the call. Setting \code{AnnotGPL = FALSE} and \code{getGPL = FALSE} means that GSE
downloads do not request GPL-supplied gene annotation or a separate GPL
record. code{destdir} must already exist and be writable.
}
\examples{
\dontrun{
gset <- download_gset("GSE1009", destdir = tempdir())
}

}
\seealso{
\code{\link[GEOquery:getGEO]{GEOquery::getGEO()}}
}
