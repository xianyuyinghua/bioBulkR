% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata_process.R
\name{metadata_process}
\alias{metadata_process}
\title{Interactively process GEO sample metadata}
\usage{
metadata_process(metadata = metadata)
}
\arguments{
\item{metadata}{A \code{data.frame} containing sample metadata. It must include a
\code{geo_accession} column and the column selected interactively as the sample
grouping variable. Metadata returned by \code{Biobase::pData()} is a typical
input.}
}
\value{
A \code{data.frame} with three columns:
\describe{
\item{\code{Group}}{The final group label after optional renaming.}
\item{\code{Sample}}{The sample accession taken from \code{geo_accession}.}
\item{\code{Raw_Group}}{The original group label before renaming.}
}
}
\description{
Inspect, filter, and simplify GEO sample metadata into a sample grouping
table. The function displays frequency tables for likely metadata columns,
then interactively asks the user to filter samples, choose a grouping
column, and optionally rename its group labels.
}
\details{
Candidate columns whose names begin with \code{title}, \code{geo}, \code{source},
\code{characteristics}, \code{platform}, \code{scan}, \code{data}, \code{grade}, \code{batch}, \code{tissue},
or \code{contact} are summarized at the start of the function.

Filtering instructions use the form
\code{column::value1,value2}. Multiple column filters can be separated with
semicolons, for example,
\code{column1::value1,value2;column2::value3,value4}. Values are matched
exactly, and filters for multiple columns are combined sequentially.
Press Enter without supplying a filter to retain all samples.

Group renaming instructions use the form
\code{original_pattern::new_name}, with multiple mappings separated by commas.
Original names are interpreted as regular-expression patterns by
\code{\link[base:grep]{base::grepl()}}. Press Enter to retain the original group labels.

This function calls \code{\link[base:readline]{base::readline()}} and is therefore intended for use
in an interactive R session.
}
\examples{
\dontrun{
metadata <- data.frame(
  geo_accession = c("GSM1", "GSM2", "GSM3"),
  tissue = c("tumor", "tumor", "normal"),
  stringsAsFactors = FALSE
)

groups <- metadata_process(metadata)
}
}
