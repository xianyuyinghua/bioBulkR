% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_go_kegg.R
\name{enrich_go_kegg}
\alias{enrich_go_kegg}
\title{Perform GO and KEGG enrichment analysis and create enrichment plots}
\usage{
enrich_go_kegg(
  genes,
  species,
  p_name = "p.value",
  filter_pvalue = 0.05,
  pvalueCutoff = 0.5,
  qvalueCutoff = 1,
  kegg_analysis_method = "online",
  plot_type = NULL,
  scankeyplot_mode = "sankey_buble",
  fill_colors = basicR::get_colors(number = 4.8),
  go_kegg_topn = c(10, 10),
  description_wrap_width = 100
)
}
\arguments{
\item{genes}{A character vector of gene symbols, for example
\code{c("TP53", "EGFR", "MYC")}. Duplicate and unmapped symbols are handled
by \code{\link[clusterProfiler:bitr]{clusterProfiler::bitr()}}; genes that cannot be mapped are omitted from
the enrichment analysis.}

\item{species}{A single character string containing an NCBI taxonomy ID.
Currently supported values are \code{"9606"} for human and \code{"10090"} for
mouse. Human uses \code{org.Hs.eg.db} with KEGG organism code \code{"hsa"}; mouse
uses \code{org.Mm.eg.db} with KEGG organism code \code{"mmu"}.}

\item{p_name}{A character string selecting the significance column used to
filter and display results. Use \code{"p.value"} for the raw p-value or
\code{"p.adj"} for the Benjamini-Hochberg adjusted p-value. The default is
\code{"p.value"}.}

\item{filter_pvalue}{A single numeric value between 0 and 1. After GO and
KEGG enrichment, terms with the column selected by \code{p_name} strictly less
than this value are retained. The default is \code{0.05}.}

\item{pvalueCutoff}{A single numeric value between 0 and 1 passed to
\code{\link[clusterProfiler:enrichGO]{clusterProfiler::enrichGO()}} and \code{\link[clusterProfiler:enrichKEGG]{clusterProfiler::enrichKEGG()}} as their
initial raw p-value cutoff. It should normally be greater than or equal to
\code{filter_pvalue}. The default is \code{0.5}.}

\item{qvalueCutoff}{A single numeric value between 0 and 1 passed to the
clusterProfiler enrichment functions as the q-value cutoff. The default
is \code{1}, which does not impose an additional restrictive q-value filter.}

\item{kegg_analysis_method}{A character string specifying the source of KEGG
annotations. \code{"online"} (default) queries the current KEGG data through
clusterProfiler. \code{"local"} uses the legacy internal data supplied by
\code{KEGG.db} and requires that package to be installed.}

\item{plot_type}{\code{NULL} to skip plotting, or a character vector containing
one or more of \code{"barplot"}, \code{"dotplot"}, \code{"sankeyplot"}, and
\code{"circularplot"}. Multiple plot families can be requested in one call,
for example \code{c("barplot", "dotplot")}. Each family contains separate
GO, KEGG, and combined GO/KEGG outputs. Circular plot entries are drawing
functions and must be invoked, whereas the other entries are ggplot
objects.}

\item{scankeyplot_mode}{A character string controlling the Sankey layout.
Use \code{"sankey_buble"} (default) to place genes on the left, pathways in the
middle, and the enrichment bubbles on the right. Use \code{"buble_sankey"} to
place bubbles and pathways on the left and genes on the right. The argument
name and values retain the historical spelling for backward compatibility.}

\item{fill_colors}{A character vector of colors used for ontology fills and
continuous plot scales. By default colors are obtained from
\code{basicR::get_colors(number = 4.8)}. Supply enough distinct colors for all
requested ontology classes, particularly for circular plots.}

\item{go_kegg_topn}{A positive numeric vector of length one or two controlling
the number of terms shown in each plot. A length-one value is used for both
analyses. With two values, the first is the number retained separately for
each GO ontology (BP, CC, and MF), and the second is the number retained for
KEGG. This affects plots only; the complete filtered table is returned in
\code{result}. The default is \code{c(10, 10)}.}

\item{description_wrap_width}{A positive number giving the approximate number
of characters at which pathway and ontology descriptions are wrapped in
plots. The default is \code{100}.}
}
\value{
A named list with two elements:
\describe{
\item{result}{A data frame containing the combined, filtered GO and KEGG
results. It includes ontology, term identifiers and descriptions, gene and
background ratios, enrichment statistics, p-values, mapped gene symbols,
and gene counts.}
\item{plot}{\code{NULL} when \code{plot_type = NULL}; otherwise, a nested list keyed
by plot family. Each requested family contains \code{go}, \code{kegg}, and \code{gokegg}.
Bar, dot, and Sankey entries are ggplot objects. Circular entries are
zero-argument functions that draw the plot when called.}
}
}
\description{
Converts gene symbols to Entrez identifiers, performs Gene Ontology (GO)
and Kyoto Encyclopedia of Genes and Genomes (KEGG) over-representation
analysis, combines the significant results, and optionally creates several
publication-oriented visualizations.
}
\details{
Input identifiers must be gene symbols. The function uses Benjamini-Hochberg
correction in both enrichment analyses. GO enrichment is performed for BP,
CC, and MF together. Only human and mouse annotation packages are currently
supported. Online KEGG analysis requires network access at run time.

Sankey plots require \code{ggsankey}, \code{ggnewscale}, and \code{gground}. Circular plots
require \code{circlize} and \code{ComplexHeatmap}. The \code{basicR} package supplies the
default color palette.
}
\examples{
\dontrun{
genes <- c("TP53", "EGFR", "MYC", "CDKN1A", "BAX")

# Analysis only
res <- enrich_go_kegg(
  genes = genes,
  species = "9606",
  p_name = "p.adj",
  filter_pvalue = 0.05
)
head(res$result)

# Analysis with bar and dot plots
res <- enrich_go_kegg(
  genes = genes,
  species = "9606",
  plot_type = c("barplot", "dotplot"),
  go_kegg_topn = c(10, 10)
)
res$plot$barplot$gokegg
}

}
