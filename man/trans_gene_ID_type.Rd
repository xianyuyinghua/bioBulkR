% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans_gene_ID_type.R
\name{trans_gene_ID_type}
\alias{trans_gene_ID_type}
\title{Convert gene identifiers to gene symbols}
\usage{
trans_gene_ID_type(
  gene_ID_type = gene_ID_type,
  useMart_dataset = useMart_dataset,
  gene_id = gene_id,
  species_OrgDb = species_OrgDb,
  gencode_annotation = "/mnt/g/BioinforMationData/Annotation/gencode.v36.annotation.csv",
  gene_type = "protein_coding"
)
}
\arguments{
\item{gene_ID_type}{A single character string specifying the input
identifier type. Supported values are \code{"UCSC"}, \code{"ENSEMBL"},
\code{"ENTREZID"}, \code{"REFSEQ"}, and \code{"SYMBOL"}.}

\item{useMart_dataset}{A character string naming the Ensembl BioMart data
set, for example, \code{"hsapiens_gene_ensembl"}. This argument is used only
when \code{gene_ID_type = "UCSC"}.}

\item{gene_id}{A vector of gene identifiers to convert.}

\item{species_OrgDb}{A character string naming the organism annotation
package used by \code{\link[clusterProfiler:bitr]{clusterProfiler::bitr()}}, such as \code{"org.Hs.eg.db"} or
\code{"org.Mm.eg.db"}. For human Ensembl identifiers, the value
\code{"org.Hs.eg.db"} selects the local GENCODE annotation branch.}

\item{gencode_annotation}{A character string giving the path to a
comma-separated GENCODE annotation file. The file must contain
\code{ensembl_id}, \code{gene_name}, and \code{gene_type} columns. It is used only for
human Ensembl identifiers when \code{species_OrgDb = "org.Hs.eg.db"}.}

\item{gene_type}{A character string specifying the GENCODE gene biotype to
retain. The default is \code{"protein_coding"}.}
}
\value{
A two-column object with columns \code{Gene_id} and \code{Symbol}. Identifiers
without a matching annotation may be omitted. One input identifier can
produce multiple rows when multiple mappings are available.
}
\description{
Convert UCSC, Ensembl, Entrez, or RefSeq gene identifiers to gene symbols.
Gene symbols can also be supplied directly to obtain a standardized
two-column mapping table.
}
\details{
Conversion is performed differently for each identifier type:
\itemize{
\item \code{UCSC} uses \code{\link[biomaRt:useMart]{biomaRt::useMart()}} and \code{\link[biomaRt:getBM]{biomaRt::getBM()}} and therefore
requires an internet connection.
\item Human \code{ENSEMBL} identifiers use the local file supplied through
\code{gencode_annotation} when \code{species_OrgDb = "org.Hs.eg.db"}.
\item Other \code{ENSEMBL}, \code{ENTREZID}, and \code{REFSEQ} identifiers use
\code{\link[clusterProfiler:bitr]{clusterProfiler::bitr()}} with \code{species_OrgDb}.
\item \code{SYMBOL} identifiers are returned unchanged in the standardized table.
}
}
\examples{
\dontrun{
trans_gene_ID_type(
  gene_ID_type = "ENTREZID",
  gene_id = c("7157", "672"),
  species_OrgDb = "org.Hs.eg.db"
)

trans_gene_ID_type(
  gene_ID_type = "UCSC",
  useMart_dataset = "hsapiens_gene_ensembl",
  gene_id = c("uc001gzh.2", "uc003tqk.3"),
  species_OrgDb = "org.Hs.eg.db"
)
}
}
\seealso{
\code{\link[=identify_gene_ID_type]{identify_gene_ID_type()}}, \code{\link[clusterProfiler:bitr]{clusterProfiler::bitr()}},
  \code{\link[biomaRt:getBM]{biomaRt::getBM()}}
}
