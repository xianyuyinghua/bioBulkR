% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_array_raw_gset.R
\name{download_array_raw_gset}
\alias{download_array_raw_gset}
\title{Download and load raw Affymetrix data from GEO}
\usage{
download_array_raw_gset(data_id, destdir)
}
\arguments{
\item{data_id}{A single GEO Series accession supplied as a character string,
for example, \code{"GSE1009"}.}

\item{destdir}{A character string giving the existing directory in which
supplementary files are downloaded and extracted CEL files are located.}
}
\value{
An \code{\link[affy:AffyBatch-class]{affy::AffyBatch}} object when CEL files are successfully loaded;
otherwise \code{NULL}.
}
\description{
Download the supplementary files associated with a Gene Expression Omnibus
(GEO) Series and load its extracted Affymetrix CEL files into an
\code{\link[affy:AffyBatch-class]{affy::AffyBatch}} object.
}
\details{
The function first calls \code{\link[GEOquery:getGEOSuppFiles]{GEOquery::getGEOSuppFiles()}} to download all
supplementary files for \code{data_id} into \code{destdir}. It then asks the user to
extract the downloaded archive. If the automatic download fails, the user
is prompted to download and extract the raw files manually before loading
them with \code{\link[affy:ReadAffy]{affy::ReadAffy()}}.

The extracted CEL files (including files ending in \code{.CEL.gz}) must
be located directly inside \code{destdir}, rather than in a nested directory.
This function is intended for Affymetrix data supported by the \code{affy}
package and is interactive because it asks the user to confirm that file
extraction or manual downloading is complete. The download timeout is set
to 240 seconds for the duration of the call.
}
\examples{
\dontrun{
raw_gset <- download_array_raw_gset("GSE1009", destdir = tempdir())
}
}
\seealso{
\code{\link[GEOquery:getGEOSuppFiles]{GEOquery::getGEOSuppFiles()}}, \code{\link[affy:ReadAffy]{affy::ReadAffy()}},
  \code{\link[=download_gset]{download_gset()}}
}
